# docker-compose.yml - Deploy Nginx to serve the static status website
services:
  web:
    image: nginx:alpine
    container_name: yups-status
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
